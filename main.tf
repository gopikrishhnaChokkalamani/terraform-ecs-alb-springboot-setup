provider "aws" {
  region  = "us-east-1"   # Set your preferred AWS region
 # access_key = "your_access_key"  # Optional: AWS Access Key (If you're using access keys directly)
 # secret_k= "your_secret_key"  # Optional: AWS Secret Key (If you're using access keys directly)
 #  profile = "your_profile"  # Optional: AWS profile name (if using AWS CLI profile)
}
